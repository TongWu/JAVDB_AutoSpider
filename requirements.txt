requests>=2.25.1
beautifulsoup4>=4.9.3
lxml>=4.6.3
pikpakapi>=0.1.11
curl_cffi>=0.7.0
fastapi>=0.110.0
uvicorn>=0.27.0

# Optional dependencies
# For SOCKS5 proxy support (optional):
#   pip install requests[socks]
# For OCR-based captcha solving (optional):
#   pip install pytesseract Pillow
#   Also requires: tesseract-ocr system package

# --- Test / dev dependencies (optional for CI and local testing) ---
pytest>=7.0.0
pytest-cov>=4.0.0
coverage>=7.0.0
flake8>=6.0.0