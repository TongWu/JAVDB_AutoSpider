1. 对于主页面：
    - 增加一栏“任务历史”，里面要列出所有正在运行或之前运行的任务id，模式，如果是adhoc的话则还需要包含url。包含下一个计划任务的信息。
    - 运行状态中，定期任务的成功和失败次数似乎没有和db文件连接上，当前显示都为-
2. 对于定期任务：
    - 将提交任务按钮移到最下方
    - 将停止轮询移到运行日志中
    - 如果是pipeline的运行日志，pipeline会调用其他的脚本例如spider等。这个时候日志只会在spider运行完成之后一次性输出，请更改为也滚动输出。（可能需要修改后端读取日志的方法或修改pipeline）
3. 对于手动任务：
    - 将提交任务按钮移到最下方
4. 对于配置管理：
    - Git:
        - 仓库地址不需要浏览文件按钮
    - 代理：
        - 两个只读选项不用显示在前端里面
    - Cloudflare：
        - 需要支持对每个代理/本机启用自定义端口 （这个可能需要改动核心代码）
    - JavDb登录：
        - 最后五项超时选项都不是这个tab下面的，需要移到别的或新建一个tab
    - Rclone：
        - 用户只需要输入明文的rclone config，在他输入并保存后，自动转换为base64并显示。增加一个按钮解密为明文可以让用户对比
5. 对于任务日志：
    - 用户记不住任务id是什么，与其让用户输入id，不如直接把所有任务展示给他们让他们自己选择
6. 其他样式：
    - 当有任务在运行的时候，右下角会弹出一个框，这个框是黑色的，字也是黑色的，需要修改否则看不清